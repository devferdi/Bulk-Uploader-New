# Required for all stores
# You can use either the bare store name or the full myshopify domain.
store_name=your-store
shopify_api_version=2026-01

# Preferred for client stores: OAuth backend flow
# oauth_backend_url=https://shopify-bulk-oauth.youragency.com
# agency_api_key=replace-with-your-agency-api-key

# Same-organization Dev Dashboard app auth
# shopify_client_id=replace-with-your-dev-dashboard-client-id
# shopify_client_secret=replace-with-your-dev-dashboard-client-secret

# Optional legacy fallback for stores not migrated yet
# access_token=shpat_legacy_token_here

# Existing AI workflow
openai_api_key=replace-with-your-openai-api-key
